package com.akmz.springBase.common.mapper;

import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface BaseMapper {
    // This is a dummy interface to ensure MyBatis recognizes the BASE namespace.
    // It does not need any methods as Base.xml only contains SQL snippets.
}
