<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.akmz.springBase.admin.mapper.UserRoleMapper">

    <resultMap id="userRoleResultMap" type="com.akmz.springBase.admin.model.entity.UserRole">
        <id property="id" column="ID"/>
        <result property="userName" column="USER_NAME"/>
        <result property="roleId" column="ROLE_ID"/>
    </resultMap>

    <select id="findUserRolesByUserName" resultMap="userRoleResultMap">
        SELECT ID, USER_NAME, ROLE_ID
        FROM USER_ROLES
        WHERE USER_NAME = #{userName}
    </select>

    <insert id="insertUserRole" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO USER_ROLES (USER_NAME, ROLE_ID)
        VALUES (#{userName}, #{roleId})
    </insert>

    <delete id="deleteUserRole">
        DELETE FROM USER_ROLES
        WHERE USER_NAME = #{userName} AND ROLE_ID = #{roleId}
    </delete>

</mapper>
